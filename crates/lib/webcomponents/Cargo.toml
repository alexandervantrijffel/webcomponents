[package]
name = "webcomponents"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
askama = { workspace = true }
chrono = { version="0.4", default-features=false, features=["alloc"] }

[features]
assets = []
